Build site: context/plans/build-site-token-efficiency.md
# T-te-104: Atomic rewind — checkout + JSONL truncate + summary reconstruction
**Status:** done
`rewindSession(checkpoints, targetEntryId, adapter)` in `shadow-git.ts`. Adapter supplies checkout/truncate/reconstruct primitives. Happy path: checkout commit, truncate JSONL at entry, reconstruct summary, return `{status: "ok", summary}`.
Test: "successful rewind returns summary + ok".
Acceptance R2 AC-1 met.
