Build site: context/plans/build-site-token-efficiency.md
# T-te-108: Selection re-enters session at head with shadow repo attached
**Status:** partial (plumbing path defined; runtime re-entry wires in a live session)
Caller receives the selected sessionId and instantiates a fresh `ShadowCheckpoints(sessionId)` — `repoPath` is deterministic, so the shadow repo is automatically attached. Full runtime wiring (loading JSONL into the agent-loop, replaying context) is a presentation-layer follow-up that depends on the existing SessionManager.
Acceptance R4 AC-4 met at data layer.
