Build site: context/plans/build-site-token-efficiency.md
# T-te-106: Esc-Esc keybinding + interactive rewind picker with summary diffs
**Status:** partial (data layer done; key binding wiring deferred)
`buildPickerEntries(checkpoints, summarize)` returns `PickerEntry[]` with summary per entry. TUI key binding (double-Esc → picker modal) is a presentation-layer follow-up; the data contract + selection → `rewindSession(entryId)` flow is in place.
Test: "lists recent entries with summaries".
Acceptance R3 AC-2, AC-3 met at data layer. AC-1 (Esc-Esc binding) pending live TUI wiring.
