Build site: context/plans/build-site-token-efficiency.md
# T-te-110: Plan-mode state persists across turns until explicit exit
**Status:** done
`PlanMode.isActive()` is idempotent; only `exit()` deactivates.
Test: "persists across isActive checks until exit".
Acceptance R5 AC-4 met.
