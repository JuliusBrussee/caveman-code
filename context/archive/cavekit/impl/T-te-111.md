Build site: context/plans/build-site-token-efficiency.md
# T-te-111: Checkpoint GC retention with active-session protection
**Status:** done
`selectGcCandidates(dirs, { retentionDays, activeSessionId }, now)` returns dirs older than cutoff, excluding the active session.
Tests: 30-day default (older repos eligible, recent spared, active protected), configurable retention days.
Acceptance R7 AC-1, AC-2, AC-3 met.
