Build site: context/plans/build-site-token-efficiency.md
# T-te-107: cave resume fuzzy picker over ~/.cave/sessions/ with metadata rows
**Status:** done
`SessionRow` shape: `{sessionId, lastActivity, messageCount, title, dollars}`. `sortByRecency()` orders newest first. `fuzzyFilter(rows, query)` narrows on sessionId or title substring (case-insensitive).
Tests: sort order, all four display fields, fuzzy narrowing on title + sessionId.
Acceptance R4 AC-1, AC-2, AC-3 met.
