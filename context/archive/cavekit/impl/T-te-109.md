Build site: context/plans/build-site-token-efficiency.md
# T-te-109: Plan mode blocking write/edit/bash, preserving grep/read
**Status:** done
`PlanMode` class with `enter()`, `exit()`, `isActive()`, `isBlocked(tool)`, `isAllowed(tool)`. Blocked set: write/edit/apply_sr_diff/edit_symbol/bash. Allowed set: read/grep/glob/ls/find.
Tests: blocking, allowing, inactive passthrough.
Acceptance R5 AC-1, AC-2, AC-3 met. In plan mode no mutating tools run, so no shadow commits are produced either.
