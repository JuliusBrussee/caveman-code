Build site: context/plans/build-site-token-efficiency.md
# T-te-105: Truncation-failure rollback + missing-entry-id error path
**Status:** done
On adapter error during truncate, `rewindSession` catches, re-checks out the latest checkpoint (best-effort rollback), returns `{status: "rollback", error}`. Missing entry id short-circuits with `{status: "not_found"}` and no adapter calls.
Tests: "nonexistent entry id returns not_found with no mutation", "truncation failure triggers rollback checkout".
Acceptance R2 AC-2, AC-3 met.
